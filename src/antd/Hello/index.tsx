import { Button } from "antd";
import React, { FC } from "react";
import "./index.css";

const Hello: FC = () => {
    return (
        <div>
            <Button type="primary">btn</Button>
        </div>
    );
}

export default Hello;
